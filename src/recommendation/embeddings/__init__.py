"""Sentence Transformer product text encoding and embedding cache.

Implemented in Phase 3. Produces frozen ~384-D semantic product
representations, precomputed and cached (not fine-tuned on the small
synthetic catalog).
"""
